li = []
names = ['bob', 'alice', 'john']
length = len(li)

print(f"This list has {length} items")

for name in names:
    li.append(names)
    string = f"{name}"
    print(string)

lengthh = len(li)
print(f"This list now has {lengthh} items")

